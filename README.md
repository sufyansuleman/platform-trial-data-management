# Platform trial data management

A working reference implementation of the data management function for a multi-site,
multi-country, multi-domain adaptive platform trial: everything between a messy EDC export
and a frozen, verifiable dataset that an analysis can be run on.

> **Disclaimer.** This is an independent educational project. All data is synthetic and
> generated by the code in this repository. It is not affiliated with, derived from, or
> endorsed by any real clinical trial, hospital, or research group. No real patient data is
> used or expected.

---

## The problem it solves

Twenty-five hospitals in five countries enter data into an EDC. Every night it exports one
CSV per site per form. Those files disagree with each other in every way a file can:

- Three of the five countries write dates one way, two write them the other
- Two countries use a decimal comma, three a decimal point
- One site exports latin1 and has not mentioned it
- Weights arrive in pounds from sites that were told kilograms
- Sites activated nine months apart, so a naive completeness table punishes the new ones

Somebody has to turn that into a dataset a committee can make a decision on, know which
parts of it are trustworthy, tell twenty-five sites what to fix, and be able to prove a year
later that the dataset a committee saw can be regenerated exactly.

**That is the job this repository does.** It also simulates the trial that produces the
mess, because a data management pipeline with no data to manage cannot be demonstrated or
tested.

## What it does

```text
config/trial.yml ─┐
                  ├─→ simulate ─→ inject defects ─→ EDC export (125 CSVs, local formats)
config/schema/ ───┘                    │                        │
                                       │                        ▼
                                  ground truth              ingest  ── conformance log
                                       │                        │
                                       │                        ▼
config/rules/ ────────────────────→ validate ──────────→  findings table
                                       │                        │
                                       ▼                        ▼
                                  recall score            derived endpoint
                                                                │
                                                                ▼
                                                  25 site reports + central report
                                                                │
                                                                ▼
                                                  frozen data cut + SHA-256 manifest
```

At the scale it currently runs: **2,549 participants**, **25 sites**, **5 countries**,
**3 clinical domains** over a shared population, **24 months** of staggered enrolment,
**23 validation rules**, **13 injected defect types**, **419 tests**.

Every dataset is generated from a seed. Nothing is committed that the code cannot rebuild.

---

## Start here

| If you want to | Go to |
|---|---|
| Get it running on your machine | [Run it](#run-it) below, then [runbook](docs/runbook.md) section 1 |
| Operate it: an export has arrived, now what | [runbook](docs/runbook.md) section 2 |
| Know what is built and what is deliberately not | [data management plan](docs/data_management_plan.md) |
| Understand why something is shaped the way it is | [decisions](docs/decisions.md) |
| Add or change a validation rule | [runbook](docs/runbook.md) section 3 |
| Look up a field, its type and its bounds | [data dictionary](docs/data_dictionary.md) |
| Know what happens when a rule fires | [validation plan](docs/validation_plan.md) |
| Produce, verify or reproduce a data cut | [cut SOP](docs/data_cut_sop.md) |
| Point this at a different trial entirely | [adapting](docs/adapting.md) |

If you read only one document, read **[docs/decisions.md](docs/decisions.md)**. It is a
dated log of every non-obvious choice with the reasoning and the alternative rejected,
including the bugs found and what they taught. Most questions of the form "why on earth is
it done like that" are answered there.

## Run it

```bash
Rscript -e 'renv::restore()'
Rscript -e 'targets::tar_make()'
Rscript -e 'testthat::test_dir("tests/testthat")'
```

> **On R 4.4, set the binary preference first.** CRAN classifies R 4.4 as oldrel and no
> longer builds fresh binaries for it, so a plain `renv::restore()` silently starts
> compiling `igraph` and `arrow` from source and takes about two hours. It looks like a
> hang; it is a compile.
>
> ```r
> options(pkgType = "binary", install.packages.check.source = "no")
> renv::restore()
> ```
>
> Versions are pinned by `renv.lock` either way, so only the build time changes
> ([DEC-003](docs/decisions.md)). Not needed on R 4.5 or later.

The pipeline takes about 15 seconds, the test suite about 30. Then, optionally,
`Rscript scripts/render_reports.R` builds all 26 reports into `_site/` in about 3.5 minutes,
or pass a site ID to build just one.

---

## What is built, and what is not

The pipeline is finished at its own boundary and nothing in it is a stub: ingest, conform,
validate, score recall, derive the endpoint, monitor drift, freeze a cut, prove it
regenerates, reconcile allocation.

**The statistical analysis is deliberately out of scope.** The frozen data cut is the
handover point: producing it and proving it reproducible is data management, and what is
done with it afterwards is not ([DEC-025](docs/decisions.md)).

Several data management components are **specified but not implemented**, each with a
stated design rather than a silence. The largest is query management: findings exist and
nothing yet turns them into queries with a clock on them. Post-lock change detection and
risk-based central monitoring are the other two.

Every section of the **[data management plan](docs/data_management_plan.md)** is marked
implemented or specified-not-implemented, and it ends with the open work in the order that
unblocks the most. That is the honest picture, in one place.

---

## How it works, and the decisions behind it

### The rules are data, not code

No R code knows what a vasopressor is, or that discharge follows admission. Every check
lives in `config/rules/*.yml` as an expression, a severity, a description and a rationale:

```yaml
- id: TMP-001
  name: discharge_not_before_admission
  scope: outcome_30d
  severity: critical
  description: >
    Hospital discharge must not precede ICU admission.
  rationale: >
    An impossible ordering means one of the two dates is wrong, and length of
    stay derived from them is meaningless until the site says which.
  expression: is.na(hospital_discharge_date) | hospital_discharge_date >= icu_admission_date
  action: query
```

A clinician or trial manager can read the whole rule set and propose a change without
reading any R. Cross-form facts are precomputed as named columns, so rules read as prose
rather than as joins. A rule that cannot be evaluated does not fire, because reporting a
missing field twice sends the site two queries for one problem.

### The rules are scored, not trusted

The pipeline injects a catalogue of known defects and records ground truth for every one,
so the engine's performance is **measured rather than asserted**, including what it misses.

| Defect | Rule | Injected | Detected | Recall |
|---|---|---:|---:|---:|
| D01 missing required field | STR-001 | 2,635 | 2,625 | 99.6% |
| D02 gap in daily records | LOG-001 | 46 | 45 | 97.8% |
| D03 implausible weight | RNG-001 | 16 | 16 | 100% |
| D03 implausible heart rate | RNG-002 | 103 | 103 | 100% |
| D03 implausible temperature | RNG-003 | 88 | 87 | 98.9% |
| D04 discharge before admission | TMP-001 | 25 | 24 | 96.0% |
| D05 AE before randomisation | TMP-004 | 23 | 23 | 100% |
| D06 duplicate participant ID | STR-003 | 6 | 5 | 83.3% |
| D07 double randomisation | STR-004 | 8 | 8 | 100% |
| D08 alive after death date | LOG-002 | 14 | 14 | 100% |
| **D11 pounds submitted as kg** | RNG-001 | 31 | 23 | **74.2%** |
| D09 entry-delay drift | *none* | 739 | 0 | n/a |
| D10 terminal-digit preference | *none* | 453 | 0 | n/a |
| D12 AE under-reporting | *none* | 17 | 0 | n/a |

**Overall: 99.3%** of defect records that a rule targets.

Three points of honesty in that table:

- **D11 at 74.2% is a genuine limitation.** A pounds reading submitted as kilograms is only
  implausible if the patient was heavy enough: 60 kg × 2.2 = 132 kg sits inside any
  defensible plausibility bound. The rest needs a site-level distribution check, not a row
  rule.
- **D09, D10 and D12 are `n/a`, not 0%.** No rule targets them. Scoring a rule set against
  defects it was never written to catch would misrepresent it; omitting them would
  misrepresent the defect catalogue. They are statistical signals, and central monitoring is
  on the open scope list.
- **LOG-001 fires 138 times against 46 injected gaps.** Most of the excess is real: no form
  carries an ICU discharge date, so a gap from readmission is indistinguishable from missing
  entry. The fix is a schema change, not a rule change.

**An unplanned dividend.** Defect D06 issues the same participant identifier at two
different sites, and was built purely to exercise the key checks. Months later it surfaced a
real bug somewhere it was never aimed: an unguarded join that would have given those
duplicated participants double weight. Synthetic defects designed to test one component
become a standing adversarial dataset for everything built afterwards.

### Nothing is silently coerced

Sites export in their own conventions and ingest has to undo all of it, logging every
transformation:

| Transformation | Sites | Values |
|---|---:|---:|
| Date format normalised to ISO | 20 | 46,962 |
| Blank read as missing | 25 | 7,109 |
| Decimal comma normalised | 5 | 3,653 |
| Latin-1 transcoded to UTF-8 | 1 | 1,339 |
| Units converted (lb to kg, mg/dL to µmol/L) | 2 | 270 |

Encoding is detected from the file bytes, not trusted from configuration, because a site
that changes its export settings will not tell you. A value that cannot be interpreted under
an explicit rule **stops the pipeline**, naming the site, the field and the offending values.
That is the first thing a new operator hits, it is correct behaviour, and the
[runbook](docs/runbook.md) section 4 explains how to work through it.

### The endpoint reports how much of itself is guesswork

`days_alive_without_life_support` is the number a stopping decision reads. An error in it
does not produce a wrong report, it produces a wrong decision about whether to keep
randomising patients. It has **40 tests**, written before the function: death at days 0, 1,
29, 30 and 31; partial days; interior gaps; ICU transfers; discharge and readmission;
support beginning exactly at the boundary; conflicting duplicate records; multi-domain
windows anchored to different randomisation dates.

The consequential decision is what a **missing** day means. A day with no record is
`unknown`, never "free of support", unless a documented discharge explains it. Crediting
unexplained gaps would inflate the endpoint in proportion to how badly a site enters its
data, turning a data quality problem into an apparent treatment effect
([DEC-006](docs/decisions.md)).

The function returns `unknown_days` and `complete` alongside the count, because:

| | All records | Complete only |
|---|---:|---:|
| Mean days alive without life support | 16.18 | 14.55 |

That 1.6-day gap is wider than the margin an equivalence analysis would plausibly use to
declare two arms clinically indistinguishable. A single confident-looking integer would have
hidden it.

### Drift is caught by trends, not rules

No validation rule can catch a site getting slower. Every record at a drifting site is
perfectly valid, it was simply entered late, which is not a rule violation. Fitting a slope
to each site's median entry delay, measured against **its own initiation date** rather than
calendar time, identifies SE-02 at **1.69 days per month** against 0.18 for the next worst
site. That is the injected D09 defect, found by the only method that can find it.

### A cut can be regenerated and proven identical

Each frozen cut carries a manifest recording its contents, the data quality at the moment of
freezing, and the provenance needed to rebuild it: git commit, R and package versions,
rule-set version, config hash, seed, plus a SHA-256 over every file, hashed from disk after
writing rather than from the objects in memory, because the file is what a later reader
verifies.

> Given a cut ID, this repository regenerates the exact dataset a committee saw and proves
> it identical. [The test that checks it](tests/testthat/test-cut-reproducibility.R) runs on
> every commit.

The same test file asserts the guarantee is **falsifiable**: change one input and the hashes
must diverge, or the comparison proves nothing. `read_cut()` refuses to return data from a
cut that fails verification, rather than warning about it.

Records with incomplete data are included in cuts, not excluded. Dropping them would select
the analysis population on data quality, which correlates with site, which correlates with
case mix ([DEC-017](docs/decisions.md)).

### The quietest failure in an adaptive trial

When allocation probabilities are updated, they have to reach the randomisation system and
take effect. If they do not, **nothing errors**. No rule fires, no report looks wrong, and
the site's realised split is a perfectly ordinary ratio. Participants are simply randomised
on the wrong one until somebody notices.

Defect D13 injects exactly that at one site, built so nothing else can catch it: the
`allocation_ratio` field records the new ratio at the failing site too, because the
specification was issued to everybody, so the records stay internally consistent and no
validation rule has anything to object to.

Reconciliation compares realised against specified, per site, by exact binomial test:

| Site | n | Realised | Specified | p | Flagged |
|---|---:|---:|---:|---:|---|
| **DK-01** | 192 | 0.458 | 0.600 | 8.6e-05 | **yes** |
| NL-03 | 69 | 0.710 | 0.600 | 0.066 | no |
| DK-14 | 78 | 0.500 | 0.600 | 0.083 | no |

One site of 25 flagged, and it is the injected one. Pooled across all sites the deviation
would be diluted to nothing. The grouping is a power decision, and the function refuses to
pool domains that specify different probabilities rather than fitting a binomial test to a
count that is not binomial.

---

## Reports

**[Read them here](https://sufyansuleman.github.io/platform-trial-data-management/)**, no
installation required. The published site is built by CI from `main` on every push, so it
is never a screenshot of something that used to be true.

`scripts/render_reports.R` produces one report per site plus a central report for the
coordinating centre. **The actionable list comes first and the charts come second**: a
monitoring report is a work instruction, not a dashboard.

Each site report opens with "what to do this week", ranked by consequence rather than by
count, because one participant randomised twice matters more than two hundred blank
temperature fields.

## Repository layout

| Path | Contents |
|---|---|
| `config/trial.yml` | Sites, countries, domains, timeline, clinical model, defect catalogue, seed |
| `config/schema/` | One YAML per form: columns, types, bounds, required flags |
| `config/rules/` | The validation rules, by category |
| `R/simulate/` | Synthetic data generation and defect injection |
| `R/ingest/` | Reading and conforming the EDC exports |
| `R/validate/` | The rule engine, rule context, and recall scoring |
| `R/derive/` | The primary endpoint |
| `R/monitor/` | Monitoring metrics and drift detection |
| `R/cut/` | Cuts, manifests, verification, reproduction |
| `R/allocate/` | Allocation updates and reconciliation |
| `reports/` | Parameterised Quarto reports |
| `tests/testthat/` | 419 tests |
| `_targets.R` | The pipeline DAG |

Built in R with [`targets`](https://books.ropensci.org/targets/), `testthat` and Quarto.
Data manipulation uses `dplyr` throughout ([DEC-001](docs/decisions.md)).

## Documentation

- **[docs/runbook.md](docs/runbook.md)** - the operational manual. Setup, the routine cycle
  when an export arrives, what to look at first, how to add a rule or a field, what to do
  when ingest refuses a value, and what not to do.
- **[docs/data_management_plan.md](docs/data_management_plan.md)** - the governing plan,
  written the way a trial unit writes one. Every section marked implemented or specified,
  and the open scope in priority order.
- **[docs/decisions.md](docs/decisions.md)** - every design judgement, dated, with the
  reasoning and the alternative rejected.
- [docs/validation_plan.md](docs/validation_plan.md) - SOP-style: every rule, its rationale,
  its severity, and what happens when it fires. Generated from `config/`.
- [docs/data_dictionary.md](docs/data_dictionary.md) - every field. Generated from `config/`.
- [docs/data_cut_sop.md](docs/data_cut_sop.md) - how a frozen cut is produced, verified and
  reproduced.
- [docs/adapting.md](docs/adapting.md) - how to point this at a different trial.
- [docs/BUILD_SPEC.md](docs/BUILD_SPEC.md) - the original requirement statement, kept for
  the record. Not a status report.

## Adapting it to another trial

Almost none of this code knows which trial it is running. The trial lives in four
configuration surfaces; the machinery underneath mentions no clinical concept at all.

| Replace | Keep |
|---|---|
| `config/trial.yml`, `config/schema/`, `config/rules/` | `R/validate/`, `R/ingest/`, `R/cut/` |
| `R/simulate/` and `R/derive/` | `R/monitor/`, `R/allocate/` |

**[docs/adapting.md](docs/adapting.md)** walks through it: what changes, what does not, the
one trap in the schema files, and an honest list of what does not generalise.

## Licence

MIT - see [LICENSE](LICENSE).
